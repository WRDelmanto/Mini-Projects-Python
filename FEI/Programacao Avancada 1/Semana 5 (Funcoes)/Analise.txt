def analise (Lista):
    Count = 0
    Sum = 0
    
    for x in Lista:
        Count = Count + x
        Sum = Sum + 1
        
    Media = Count/Sum
    Mediana = Lista[(Sum//2)]
    Minimo = min(Lista)
    Maximo = max(Lista)
    
    return(Media, Mediana, Minimo, Maximo)